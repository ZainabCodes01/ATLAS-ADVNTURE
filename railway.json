{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "mkdir -p storage/framework/views storage/framework/cache storage/framework/sessions storage/logs && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
